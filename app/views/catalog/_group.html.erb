<% # container for all groups in index view -%>
<div id="groups">
  <%= render(TextCollections::Document::GroupComponent.with_collection(@response.groups, group_limit: @response.group_limit)) %>
</div>
